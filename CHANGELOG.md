# Changelog

## 0.0.17

- Read cursor shape, blink state, and animation interval from the maintained Shitty provider ABI.
- Pin the provider commit that exports the complete cursor snapshot and self-contained static SDK.
- Address SDK cache state by the build dependency document digest.
- Replace an older staged patch version as one runtime-tree transaction; an equal rerun is unchanged.
- Stage the manifest and process under the canonical Sidecar archive layout.
